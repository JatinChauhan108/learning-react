# Todo App

- Avoid using indices of the array as keys while making components. Either use unique ids or ids from database(if available).
- Because when some component is deleted, components are restructured and ids get changed.
- Data is stored in Local Storage as key value pairs and can be seen in Application -> Local Storage in devtools.